ALTER TABLE ${ohdsiSchema}.cohort_versions RENAME TO cohort_version;
ALTER TABLE ${ohdsiSchema}.cohort_characterization_versions RENAME TO cohort_characterization_version;
ALTER TABLE ${ohdsiSchema}.concept_set_versions RENAME TO concept_set_version;
ALTER TABLE ${ohdsiSchema}.ir_versions RENAME TO ir_version;
ALTER TABLE ${ohdsiSchema}.pathway_versions RENAME TO pathway_version;
